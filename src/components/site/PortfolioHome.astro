---
/**
 * Portfolio home shell — Lenis, nav, hero, team, footer. One React island so
 * useLenis() context is shared across interactive sections.
 */
import PortfolioHome from "./PortfolioHome.tsx";
---

<PortfolioHome client:only="react" />
