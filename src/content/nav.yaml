# The availability marquee. Everything else in the nav (routes, socials,
# overlay, scroll-spy ids) is site wiring and lives in src/lib/content/nav.ts.
nav:
  availabilityLine: available for projects from october ’26
  enabled: false
